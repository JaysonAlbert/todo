# Production Environment Variables
# These should be set by your deployment platform (Docker Swarm, Kubernetes, etc.)
# DO NOT put real values here - this is just documentation

# Application Settings
ENVIRONMENT=production
PORT=8080
LOG_LEVEL=info

# Database Configuration
POSTGRES_USER=your_prod_db_user
POSTGRES_PASSWORD=your_prod_db_password
POSTGRES_DB=your_prod_db_name
DATABASE_URL=postgres://user:password@prod-db-host:5432/dbname?sslmode=require

# Security (CRITICAL: Use strong, unique secrets)
JWT_SECRET=your-super-secure-jwt-secret-here

# Apple OAuth Configuration (Production values)
APPLE_TEAM_ID=your_apple_team_id
APPLE_CLIENT_ID=your_apple_client_id
APPLE_KEY_ID=your_apple_key_id
APPLE_KEY_PATH=/etc/secrets/apple/AuthKey_YOUR_KEY_ID.p8
APPLE_REDIRECT_URL=https://yourdomain.com/api/v1/auth/apple/callback
